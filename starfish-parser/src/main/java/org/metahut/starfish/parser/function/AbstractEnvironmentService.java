package org.metahut.starfish.parser.function;

/**
 * 好像有点脱离元数据了
 *
 * 但是这个是最上层的api 更类似业务api
 * so move to sf api
 */
public abstract class AbstractEnvironmentService<E,T> implements IEnvironmentApi<E,T> {

}
